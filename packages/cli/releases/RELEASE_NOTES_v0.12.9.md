# v0.12.9 (December 22, 2025)

### 📝 CLI UX Improvements

This release improves the CLI user experience with consistent `npx rapidkit` commands and helpful tips.

**Highlights**

- Unified command syntax to use `npx rapidkit` everywhere
- Added helpful tip after project creation: `Install globally (npm i -g rapidkit)`
- Updated README and template README files to reduce confusion

### Upgrade

```bash
npx rapidkit@latest --version  # Should show 0.12.9
```

---

Full release notes are available in `RELEASE_NOTES.md`.